# Copiar para .env (o .env real e ignorado pelo git).
# URL do backend que a app consome.
# Definido -> a app usa o backend hospedado (ex.: Railway).
# Vazio/removido -> detecao automatica do host do Metro (backend local em localhost:3000).
EXPO_PUBLIC_API_URL=https://maissaudavelbackend-production.up.railway.app
