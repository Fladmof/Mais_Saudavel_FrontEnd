export const spacing = {
  xs: 4,
  sm: 8,
  md: 12,
  lg: 16,
  xl: 24,
  xxl: 32,
  xxxl: 40,
  /** Margem lateral única em toda a app. */
  gutter: 16,
  /** Alvo de toque mínimo (dp). */
  touchMin: 48,
} as const;
